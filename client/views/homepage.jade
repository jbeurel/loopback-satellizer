h1 Todo List \o/

button(ng-click="authenticate('facebook')")
  i.fa.fa-facebook
  |  Sign in with Facebook

button(ng-click="authenticate('google')")
  i.fa.fa-google
  |  Sign in with Google

button(ng-click="getProfile()") Get Profile

pre {{ isAuthenticated }}

pre {{ profile }}
